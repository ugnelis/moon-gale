using UnityEngine;

namespace MoonGale.Runtime.Levels
{
    internal sealed class AirLevelNode : LevelNode
    {
        public AirLevelNode(Vector2Int mapPosition, Color color) : base(mapPosition, color)
        {
        }
    }
}
